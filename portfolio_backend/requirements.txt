--extra-index-url https://download.pytorch.org/whl/cpu
django
djangorestframework
django-cors-headers
channels
channels-redis
gunicorn
celery
redis
requests
yfinance
alpaca-py
pandas
numpy
scikit-learn
joblib
xgboost
skl2onnx
onnx
onnxruntime
pyarrow
optuna
transformers
torch==2.10.0+cpu
feedparser
textblob
vaderSentiment
gymnasium
finnhub-python
praw
newsapi-python
python-dotenv
prophet
openai
anthropic
google-genai
google-generativeai
pandas_ta
TA-Lib
psycopg2-binary
reportlab
plotly
shap
# lxml : requis par pd.read_html() -- absent jusqu'ici (2026-08-11), ce qui
# faisait échouer silencieusement _fetch_sp500_symbols() (portfolio/tasks.py)
# à chaque appel, avec retombée sur un fallback BLUECHIP_SYMBOLS lui-même
# jamais défini dans .env -> liste vide. _analyze_bluechip_rebounds()
# (bluechip_dip_scanner) tournait donc depuis le début sur un univers
# systématiquement vide, sans erreur visible (gère déjà le cas vide
# proprement). Ajouté pour le nouveau balayage large SP500/TSX, répare le
# même coup ce bug pré-existant.
lxml
